import { Text, Title } from "@ui5/webcomponents-react"

const ApplicationTitle = () => {
  return (
    <section>
      <Title>{'NTTflix'}</Title>
      <Text>{'Busque por filmes dentre a base de dados OMDB'}</Text>
    </section>
  )
}

export default ApplicationTitle
